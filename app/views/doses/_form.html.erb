<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, label: 'Amount' %>
  <%= f.association :ingredient %>
  <%= f.button :submit, label: 'Add new Ingredient', class: 'btn btn-primary' %>
<% end %>
